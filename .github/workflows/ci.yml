# ADR 0035: verify each change with the peer checkout in the installed layout.
name: CI
on:
  pull_request:
  push:
    branches: [main]
  merge_group:
  schedule:
    - cron: '47 4 * * *'
  workflow_dispatch:
    inputs:
      peer_ref:
        description: Optional runtime commit to test a coordinated change
        type: string
permissions:
  contents: read
concurrency:
  group: ci-${{ github.workflow }}-${{ github.event_name }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    name: Full CI
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Check out packages
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          path: packages
          persist-credentials: false
          submodules: false
          lfs: false
      - name: Check out runtime
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: thetis-agent/runtime
          ref: ${{ inputs.peer_ref || vars.THETIS_RUNTIME_REF || 'main' }}
          token: ${{ secrets.THETIS_SOURCE_TOKEN || github.token }}
          # KS-003 archives the pinned previous socket implementation from its tag.
          fetch-depth: 0
          path: runtime
          persist-credentials: false
          submodules: false
          lfs: false
      - uses: ./runtime/.github/actions/verify
      - name: Retain coverage, including failed tests
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: packages-ci-coverage-${{ github.run_id }}-${{ github.run_attempt }}
          path: reports/coverage/
          if-no-files-found: warn
          retention-days: 14
      - name: Retain diagnostics, including failed gates
        if: ${{ always() }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: packages-reports-${{ github.run_id }}-${{ github.run_attempt }}
          path: reports/
          if-no-files-found: warn
          retention-days: 14
      - name: Retain verified delivery candidate
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: packages-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: delivery/
          if-no-files-found: error
          retention-days: 14
