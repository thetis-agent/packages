{
  "name": "@thetis/kernel",
  "version": "0.6.1",
  "license": "MIT",
  "description": "The trusted service plane: who may do what. Users, identity, sessions, the pipeline, package ownership, providers, and the operator table. Not installable.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "node --test dist/test/"
  },
  "dependencies": {
    "@thetis/contracts": "^0.1.0",
    "@thetis/lib": "^0.1.0"
  }
}
