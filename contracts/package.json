{
  "name": "@thetis/contracts",
  "version": "0.1.0",
  "description": "The types and constants every part of Thetis agrees on: messages, the pipeline, packages, identity, the fence, and what package code sees. No code.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  }
}
