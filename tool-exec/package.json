{
  "name": "@thetis/tool-exec",
  "version": "0.1.0",
  "description": "Tools for the model: run commands, install, fork, remove and delete packages, and spawn subagents in your own space.",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@thetis/lib": "^0.1.0"
  },
  "peerDependencies": {
    "@thetis/contracts": "^0.1.0"
  },
  "devDependencies": {
    "@thetis/contracts": "^0.1.0"
  },
  "thetis": {
    "type": "tool",
    "tools": [
      {
        "name": "exec",
        "description": "Run a shell command (bash) inside your userspace. Working directory defaults to your home. Returns exit code, stdout and stderr.",
        "parameters": {
          "type": "object",
          "properties": {
            "cmd": {
              "type": "string",
              "description": "The command line to run"
            },
            "cwd": {
              "type": "string",
              "description": "Working directory relative to home"
            },
            "timeoutMs": {
              "type": "number"
            }
          },
          "required": [
            "cmd"
          ]
        },
        "export": "exec"
      },
      {
        "name": "install_package",
        "description": "Install a package into your userspace from a directory path (relative to home) or a git URL. The package must have a package.json with a thetis field and be scoped @<you>/<name>. It becomes live on the next turn.",
        "parameters": {
          "type": "object",
          "properties": {
            "source": {
              "type": "string"
            }
          },
          "required": [
            "source"
          ]
        },
        "export": "installPackage"
      },
      {
        "name": "uninstall_package",
        "description": "Remove an installed package from your userspace by name. Its files stay. When it was a fork that replaced another package, that package comes back.",
        "parameters": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            }
          },
          "required": [
            "name"
          ]
        },
        "export": "uninstallPackage"
      },
      {
        "name": "fork_package",
        "description": "Copy an installed package into packages/<as> under your home as @<you>/<as>, ready to edit and then install_package. Installing the fork replaces the original; uninstalling the fork puts the original back. Does not install.",
        "parameters": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "description": "The installed package to copy, such as @thetis/tools-plan"
            },
            "as": {
              "type": "string",
              "description": "The directory name under packages/; default is the unscoped name"
            }
          },
          "required": [
            "name"
          ]
        },
        "export": "forkPackage"
      },
      {
        "name": "delete_package",
        "description": "Uninstall a package in your own scope and delete its directory under packages/ in your home. When it was a fork, the original comes back. Refuses @thetis/* packages.",
        "parameters": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string"
            }
          },
          "required": [
            "name"
          ]
        },
        "export": "deletePackage"
      },
      {
        "name": "spawn_subagent",
        "description": "Start a subagent session in your userspace (same files and packages), send it a task, and return its final reply.",
        "parameters": {
          "type": "object",
          "properties": {
            "task": {
              "type": "string"
            }
          },
          "required": [
            "task"
          ]
        },
        "export": "spawnSubagent"
      }
    ]
  }
}
