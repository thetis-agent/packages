{
  "name": "@thetis/gateway-web",
  "version": "0.11.3",
  "license": "MIT",
  "description": "Your browser interface: conversations, the transcript, and the control panel, served from your own space.",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "peerDependencies": {
    "@thetis/harness-core": "^0.3.1",
    "@thetis/runtime": "^0.2.0"
  },
  "devDependencies": {
    "@thetis/harness-core": "^0.3.1",
    "@thetis/gateway-login": "^0.1.0",
    "@thetis/runtime": "file:../.."
  },
  "thetis": {
    "type": "gateway",
    "service": {
      "export": "startService"
    },
    "publish": [
      {
        "port": 8777,
        "to": "host"
      }
    ]
  }
}
