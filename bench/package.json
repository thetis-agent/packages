{
  "name": "@thetis/bench",
  "version": "0.4.1",
  "license": "MIT",
  "description": "Measures the harness: what a package makes available for a query, what it costs in bytes and round trips, and how it compares with its peers. Not installable.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "default": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "test": "node --test dist/test/"
  },
  "peerDependencies": {
    "@thetis/runtime": "^0.2.0"
  },
  "devDependencies": {
    "@thetis/runtime": "file:../.."
  }
}
